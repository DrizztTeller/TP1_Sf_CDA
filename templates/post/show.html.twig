{% extends 'base.html.twig' %} {% block title %}Post{% endblock %} {% block body
%}
<h1>Post</h1>

<table class="table">
  <tbody>
    <tr>
      <th>Title</th>
      <td>{{ post.title }}</td>
    </tr>
    <tr>
      <th>Content</th>
      <td>{{ post.content }}</td>
    </tr>
    <tr>
      <th>Image</th>
      <td>{{ post.image }}</td>
    </tr>
  </tbody>
</table>

<a href="{{ path('app_post_index') }}">back to list</a>
{% if app.user != null %}
    {% if app.user == post.author or 'ROLE_ADMIN' in app.user.roles %}
<a href="{{ path('app_post_edit', { ref: post.ref }) }}" class="btn-normal">
  {{ ux_icon("mdi:pencil", { class: "h-1 w-1" }) }}
  {{ "Edit" | trans }}
</a>
{{ include("post/_delete_form.html.twig") }}
{% endif %}
{% endif %}
 {% endblock %}
